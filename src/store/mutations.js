export default {
  setQuery(state, prop) {
    state.query = prop;
  },
  setPagination(state, prop) {
    state.pagination = prop;
  },
  setPokemons(state, prop) {
    state.pokemons = prop;
  },
};
